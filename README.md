# Morse-Chat
An app to chat in Morse code with direct translation (text mode) or just writing morse code by hand (code mode) the app supports Arabic and English with rooms for English only or Arabic only or both, the app uses a host guest system (P2P)
